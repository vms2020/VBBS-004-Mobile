<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Vbbs004Mobile" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="Theme.SplashScreen.Vbbs004" parent="Theme.SplashScreen">
        <!-- A dark, sleek background makes the blue/green logo pop -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>

        <!-- Your new SVG Vector Drawable -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_app_logo</item>

        <item name="postSplashScreenTheme">@style/Theme.Vbbs004Mobile</item>
    </style>
</resources>